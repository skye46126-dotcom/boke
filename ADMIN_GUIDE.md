# 管理后台使用指南

## 任务 7 完成总结

已成功完成**任务 7: 后台管理界面**的所有子任务：

### ✅ 已完成的功能

**7.1 文章管理列表页面**
- 创建了管理后台首页 `/admin`
- 显示文章统计信息（总数、已发布、草稿）
- 以表格形式展示所有文章
- 支持编辑和删除操作
- 实现了登录页面 `/admin/login`

**7.2 Markdown 编辑器**
- 创建了 `ArticleEditor` 组件
- 支持实时预览功能
- 集成图片上传功能
- 自动生成 URL slug
- 支持草稿和发布状态切换

**7.3 文章创建和编辑功能**
- 创建新文章页面 `/admin/articles/new`
- 编辑文章页面 `/admin/articles/[id]`
- 表单验证
- 自动保存到数据库

## 使用指南

### 1. 访问管理后台

1. 访问 `http://localhost:3000/admin/login`
2. 输入您在 `.env.local` 中配置的 `ADMIN_PATH`
3. 点击"进入管理后台"

**示例**：
如果您的 `.env.local` 中设置：
```
ADMIN_PATH=manage-panel-abc123xyz789
```
则输入：`manage-panel-abc123xyz789`

### 2. 管理后台首页

登录后，您将看到：
- **统计卡片**：显示总文章数、已发布数、草稿数
- **文章列表表格**：显示所有文章的标题、状态、创建时间、更新时间
- **操作按钮**：
  - "新建文章"：创建新文章
  - "编辑"：编辑现有文章
  - "删除"：删除文章（需确认）
  - "退出"：退出登录

### 3. 创建新文章

1. 点击"新建文章"按钮
2. 填写表单：
   - **标题**（必填）：文章标题
   - **URL Slug**（可选）：留空将自动生成
   - **摘要**（可选）：简短描述
   - **状态**（必填）：选择"草稿"或"已发布"
   - **内容**（必填）：使用 Markdown 格式编写

3. 使用编辑器功能：
   - **预览**：点击"预览"按钮查看渲染效果
   - **上传图片**：点击"上传图片"按钮上传图片到 S3
   - 图片上传成功后，Markdown 语法会自动插入到内容中

4. 点击"保存"按钮保存文章

### 4. 编辑文章

1. 在文章列表中点击"编辑"按钮
2. 修改文章内容
3. 点击"保存"按钮更新文章

### 5. 删除文章

1. 在文章列表中点击"删除"按钮
2. 确认删除操作
3. 文章将被永久删除

## Markdown 语法支持

编辑器支持完整的 Markdown 语法：

### 标题
```markdown
# 一级标题
## 二级标题
### 三级标题
```

### 文本格式
```markdown
**粗体文本**
*斜体文本*
~~删除线~~
```

### 列表
```markdown
- 无序列表项 1
- 无序列表项 2

1. 有序列表项 1
2. 有序列表项 2
```

### 链接和图片
```markdown
[链接文本](https://example.com)
![图片描述](https://example.com/image.jpg)
```

### 代码
```markdown
行内代码：`const x = 1;`

代码块：
\`\`\`javascript
function hello() {
  console.log('Hello, World!');
}
\`\`\`
```

### 引用
```markdown
> 这是一个引用块
> 可以有多行
```

### 表格
```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| A   | B   | C   |
| D   | E   | F   |
```

### 视频嵌入
```markdown
<iframe src="//player.bilibili.com/player.html?bvid=BV1xx411c7mD" 
  width="100%" height="500" frameborder="0" allowfullscreen></iframe>
```

## 图片上传

### 上传步骤
1. 点击"上传图片"按钮
2. 选择图片文件（支持 JPEG、PNG、GIF、WebP）
3. 等待上传完成
4. Markdown 语法自动插入到内容中

### 限制
- 最大文件大小：5MB
- 支持格式：JPEG、PNG、GIF、WebP
- 图片将上传到 AWS S3

### 注意事项
- 确保 `.env.local` 中配置了正确的 AWS 凭证
- 确保 S3 bucket 有公开读取权限
- 上传的图片无法通过管理后台删除（仅删除数据库记录）

## 状态管理

### 草稿（Draft）
- 不会在前台显示
- 可以随时编辑
- 适合未完成的文章

### 已发布（Published）
- 会在前台显示
- 访客可以浏览
- 可以随时切换回草稿状态

## 安全注意事项

1. **保护 ADMIN_PATH**
   - 不要在公开场合分享
   - 定期更换
   - 使用足够长的随机字符串

2. **浏览器存储**
   - adminPath 存储在 localStorage
   - 清除浏览器数据会需要重新登录
   - 不要在公共电脑上使用

3. **内容安全**
   - Markdown 内容会被清理以防止 XSS
   - 但仍需谨慎处理用户输入
   - 避免嵌入不可信的 iframe

## 故障排除

### 问题：无法登录
**解决方案**：
1. 检查 ADMIN_PATH 是否正确
2. 确保环境变量已设置
3. 重启开发服务器

### 问题：图片上传失败
**解决方案**：
1. 检查 AWS 凭证是否正确
2. 确保 S3 bucket 存在
3. 检查文件大小是否超过 5MB
4. 检查文件格式是否支持

### 问题：文章保存失败
**解决方案**：
1. 检查必填字段是否填写
2. 检查网络连接
3. 查看浏览器控制台错误信息
4. 确保数据库连接正常

### 问题：预览不显示
**解决方案**：
1. 检查 Markdown 语法是否正确
2. 刷新页面重试
3. 检查浏览器控制台错误

## 键盘快捷键

目前不支持键盘快捷键，但可以使用：
- `Ctrl/Cmd + S`：浏览器默认保存（不会保存文章）
- `Tab`：在表单字段间切换

## 下一步

根据任务列表，下一步应该实现：
- Task 8: SEO 和性能优化
  - 8.1 实现 SEO meta 标签生成
  - 8.2 配置 CDN 和性能优化

## 反馈和改进

如果您在使用过程中遇到问题或有改进建议，请：
1. 检查本文档的故障排除部分
2. 查看浏览器控制台错误信息
3. 检查服务器日志
