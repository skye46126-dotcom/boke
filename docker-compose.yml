version: '3.8'

services:
  postgres:
    image: postgres:14-alpine
    container_name: blog_postgres
    environment:
      POSTGRES_DB: personal_blog
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d personal_blog"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
